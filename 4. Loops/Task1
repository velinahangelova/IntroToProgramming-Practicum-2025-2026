#include <iostream>
using namespace std;

int main()
{
	unsigned int n;
	cin >> n;

	bool isPrime = true;
		
	for (unsigned i = 2; i * i <= n; i++) {
		if (n % i == 0) {
			isPrime = false; 
			break;
		}
	}
	cout << (isPrime ? "Prime" : "Not prime");
	
}
